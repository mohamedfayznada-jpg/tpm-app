# إعداد إشعارات WhatsApp في Factory OS TPM

## الغرض ونطاق الإشعار

تضيف هذه النسخة إشعار WhatsApp تلقائيًا عند **إسناد تاج لمهندس**، أو عند تسجيل **تاج حرج**، أو عند **تصعيد تاج إلى المراجعة**. يرسل التطبيق الطلب من الخادم فقط؛ فلا يظهر رمز Meta أو أي بيانات اعتماد في المتصفح أو Firebase أو مستودع Git.

> الإرسال الفعلي لا يبدأ إلا بعد اعتماد قالب الرسالة وإضافة متغيرات البيئة في Vercel. يتطلب مسار الإرسال بالقوالب اعتماد القالب من Meta قبل استخدامه [1]. قبل ذلك، سيواصل التطبيق تسجيل التاج بصورة طبيعية ويعرض حالة واضحة بأن الإشعارات غير مهيأة.

| المتطلب | الغرض | مكان الإعداد |
|---|---|---|
| حساب WhatsApp Business وربط رقم مرسل | تمكين الإرسال الرسمي من Meta | Meta for Developers |
| قالب Meta معتمد | إرسال الإشعار خارج نافذة خدمة العملاء | WhatsApp Manager |
| متغيرات بيئة الخادم | حفظ الرموز والمعرفات بعيدًا عن العميل | Vercel Project Settings |
| قائمة بريد المدير المصرح | منع أي مستخدم غير مخول من طلب إرسال الرسائل | Vercel Project Settings |
| أرقام مهندسين بصيغة دولية | تحديد المستلم الصحيح لكل تاج | إعدادات التطبيق |

## إنشاء قالب الرسالة

أنشئ قالبًا في WhatsApp Manager باسم مثل `tpm_tag_alert` وباللغة العربية (`ar`). يجب أن يكون القالب بحالة **Approved** قبل اختباره. يحتوي مسار الإرسال الحالي على ستة متغيرات نصية مرتبة كما يلي.

| رقم المتغير | القيمة التي يرسلها التطبيق |
|---:|---|
| `{{1}}` | رقم التاج |
| `{{2}}` | نوع الحدث: إسناد، حرج، أو تصعيد |
| `{{3}}` | القسم |
| `{{4}}` | الآلة أو المنطقة |
| `{{5}}` | الأولوية |
| `{{6}}` | وصف المشكلة |

يمكن أن تكون صياغة جسم القالب كالتالي:

```text
تنبيه TPM رقم {{1}}
الحدث: {{2}}
القسم: {{3}}
المعدة: {{4}}
الأولوية: {{5}}
الوصف: {{6}}
يرجى مراجعة التاج واتخاذ الإجراء المناسب في Factory OS.
```

## إضافة المتغيرات في Vercel

من لوحة Vercel، افتح المشروع ثم انتقل إلى **Settings → Environment Variables**. أضف المتغيرات التالية إلى بيئتي **Production** و**Preview**، ثم أعد النشر. لا تضف هذه القيم إلى ملف واجهة أو إلى Firebase.

| المتغير | القيمة المطلوبة |
|---|---|
| `WHATSAPP_ACCESS_TOKEN` | رمز وصول Meta طويل الأجل للحساب والرقم المرسل |
| `WHATSAPP_PHONE_NUMBER_ID` | Phone Number ID من لوحة Meta |
| `WHATSAPP_TEMPLATE_NAME` | اسم القالب المعتمد، مثل `tpm_tag_alert` |
| `WHATSAPP_TEMPLATE_LANGUAGE` | `ar` أو رمز لغة القالب الذي اعتمدته Meta |
| `WHATSAPP_GRAPH_API_VERSION` | `v23.0` ما لم تتطلب Meta إصدارًا أحدث |
| `FIREBASE_PROJECT_ID` | `tpm-audit-system` |
| `TPM_NOTIFICATION_ALLOWED_EMAILS` | بريد أو أكثر للمديرين المصرح لهم، مفصول بفواصل |

مثال لقيمة الصلاحية:

```text
TPM_NOTIFICATION_ALLOWED_EMAILS=mfayez@tpm.app,admin@your-factory.example
```

## ضوابط الأمان والتشغيل

تتحقق الدالة الخلفية من رمز Firebase ID Token، ومن مشروع Firebase، ومن البريد المصرح له قبل الاتصال بـ Meta. كما تتحقق من نوع الحدث ورقم المستلم وبيانات التاج، وتقبل القوالب المعتمدة فقط. لا تستخدم أي مفتاح API داخل `app.js` أو `index.html` أو قاعدة البيانات.

من صفحة الإعدادات، يستطيع المدير ضبط قواعد التعيين والتاج الحرج والتصعيد. تُحفظ هذه القواعد في Firebase ضمن `notification_settings` بصلاحية كتابة خاصة بالمدير. ويمكن للمدير أو المراجع تحديث تقدم الأنشطة المعيارية لفرق TPM ضمن `team_activity_progress`.

بعد النشر، أضف رقم WhatsApp دوليًا في سجل مهندس الصيانة، ثم أنشئ تاج اختبار منخفض الأثر أو استخدم زر **تنبيه** من بطاقة تاج موجود. إذا رفضت Meta الطلب، راجع حالة اعتماد القالب وترتيب متغيراته وربط الرقم المرسل والمستلم في حساب WhatsApp Business.

## المراجع

[1]: https://developers.facebook.com/documentation/business-messaging/whatsapp/templates/overview "Meta — WhatsApp Templates Overview"
